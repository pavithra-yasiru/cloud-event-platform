apiVersion: v1
kind: Service

metadata:
  name: clickhouse

spec:
  selector:
    app: clickhouse

  type: NodePort

  ports:

    - name: http
      port: 8123
      targetPort: 8123
      nodePort: 30088

    - name: native
      port: 9000
      targetPort: 9000
