apiVersion: v1
kind: Service

metadata:
  name: event-service

spec:
  selector:
    app: event-service
    version: blue

  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30081

  type: NodePort
